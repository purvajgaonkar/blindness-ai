services:
  - type: web
    name: retinalai
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --chdir webapp app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.0